# 🚀 دليل الاختبار السريع - Smart Measurement

## الخطوة 1: التحضير

```powershell
cd C:\Users\HP\PycharmProjects\smart-measurement
flutter clean
flutter pub get
```

## الخطوة 2: التحقق من الملفات

```powershell
# تحقق من وجود النموذج
Test-Path assets\models\best.onnx
# يجب أن يرجع: True

# اعرض حجم النموذج
(Get-Item assets\models\best.onnx).Length / 1MB
# يجب أن يكون حوالي ~12 MB
```

## الخطوة 3: تشغيل التطبيق

```powershell
flutter run
```

**أو على جهاز محدد:**
```powershell
# عرض الأجهزة المتاحة
flutter devices

# التشغيل على جهاز معين
flutter run -d <device-id>
```

## الخطوة 4: اختبار المعايرة

### 4.1 التحضير:
- [ ] احضر بطاقة ID قياسية (85.6mm × 53.98mm)
- [ ] اختر مكاناً بإضاءة جيدة
- [ ] ضع البطاقة على سطح مستوٍ داكن اللون (لتباين أفضل)

### 4.2 خطوات الاختبار:
1. افتح التطبيق
2. انتقل إلى "Smart Calibration"
3. اضغط زر "Start Calibration"
4. وجه الكاميرا نحو البطاقة
5. راقب:
   - ظهور 4 دوائر على زوايا البطاقة (TL, TR, BR, BL)
   - رسالة الحالة تتغير
   - شريط التقدم يتقدم من 0% إلى 100%
6. حافظ على الثبات حتى يكتمل
7. تحقق من النتيجة: "Scale: X.XXXX mm/px"

### 4.3 القيم المتوقعة:
- **mm_per_pixel:** بين 0.05 إلى 0.2 (حسب بعد الكاميرا)
- **Confidence:** أكبر من 0.25
- **Time to complete:** 3-5 ثواني

## الخطوة 5: مراقبة Logs

### في Terminal منفصل:
```powershell
flutter logs
```

### ابحث عن:
```
✓ OnnxInferenceService initialized
✓ Input shapes: [[1, 3, 640, 640]]
✓ Card detected with confidence: 0.XXX
✓ Calibration completed: X.XXXX mm/pixel
```

## الخطوة 6: استكشاف المشاكل

### مشكلة: التطبيق لا يبدأ
```powershell
# تحقق من أن Flutter محدث
flutter doctor

# أعد بناء التطبيق
flutter clean
flutter pub get
flutter run
```

### مشكلة: "Model not found"
```powershell
# تحقق من pubspec.yaml
code pubspec.yaml
# تأكد من وجود:
#   assets:
#     - assets/models/best.onnx

# أعد البناء
flutter clean
flutter pub get
```

### مشكلة: الكاميرا لا تعمل
```powershell
# على Android: تحقق من الأذونات في AndroidManifest.xml
# على iOS: تحقق من Info.plist
```

### مشكلة: "No valid card detection"
**الحلول:**
1. حسّن الإضاءة
2. استخدم سطح داكن تحت البطاقة
3. تأكد من أن البطاقة مسطحة
4. خفض `confidenceThreshold` في `onnx_inference_service.dart`:
   ```dart
   static const double confidenceThreshold = 0.15; // كان 0.25
   ```

### مشكلة: الأداء بطيء
**الحلول:**
1. خفض `inputSize` في `onnx_inference_service.dart`:
   ```dart
   static const int inputSize = 320; // كان 640
   ```
2. تحقق من أن الجهاز يدعم GPU
3. أغلق التطبيقات الأخرى

## الخطوة 7: قياس الأداء

### في الكود، أضف timing:
```dart
// في _processFrame
final sw = Stopwatch()..start();
final result = await _onnxService.runInference(image);
sw.stop();
debugPrint('Inference time: ${sw.elapsedMilliseconds}ms');
```

### القيم المستهدفة:
- **Inference time:** < 100ms على معظم الأجهزة
- **FPS:** > 10 إطار/ثانية
- **Memory usage:** < 200 MB

## الخطوة 8: اختبارات إضافية

### 8.1 اختبار على بطاقات مختلفة:
- [ ] بطاقة ID وطنية
- [ ] رخصة قيادة
- [ ] بطاقة ائتمان (بدون بيانات حساسة!)
- [ ] بطاقة عمل

### 8.2 اختبار في ظروف مختلفة:
- [ ] إضاءة ساطعة
- [ ] إضاءة خافتة
- [ ] في الخارج (ضوء الشمس)
- [ ] زوايا مختلفة (لا تتجاوز 45 درجة)

### 8.3 اختبار على أجهزة مختلفة:
- [ ] Android (مختلف الإصدارات)
- [ ] iOS (مختلف الإصدارات)
- [ ] أجهزة منخفضة/متوسطة/عالية المواصفات

## الخطوة 9: التوثيق

### سجل النتائج:
```
Device: _________________
Android/iOS Version: ____
Test Date: ______________

✓ Model loaded successfully
✓ Camera initialized
✓ Card detected in: ____ seconds
✓ mm_per_pixel: ________
✓ Confidence: __________
✓ Average FPS: _________

Notes:
_________________________
_________________________
```

## الخطوة 10: الإبلاغ عن المشاكل

### إذا وجدت مشكلة:
1. التقط screenshot
2. انسخ logs ذات الصلة
3. سجل:
   - نوع الجهاز
   - إصدار النظام
   - الخطوات لإعادة إنتاج المشكلة
   - السلوك المتوقع vs الفعلي

---

## ✅ Checklist الاختبار الكامل

- [ ] التطبيق يبدأ بدون أخطاء
- [ ] النموذج يتم تحميله بنجاح
- [ ] الكاميرا تعمل
- [ ] الزوايا تُكتشف وتُعرض
- [ ] شريط التقدم يعمل
- [ ] المعايرة تكتمل بنجاح
- [ ] قيمة mm_per_pixel معقولة
- [ ] يمكن إعادة المعايرة
- [ ] زر "Continue" يعمل
- [ ] الأداء مقبول (>10 FPS)

---

## 🎉 عند النجاح

إذا اكتملت جميع الاختبارات بنجاح:
1. ✅ النموذج يعمل بشكل صحيح
2. ✅ Smart Calibration Pipeline مكتمل
3. ✅ جاهز للانتقال إلى Body Tracking

## 🚀 الخطوة التالية

```powershell
# افتح التوثيق للمرحلة القادمة
code docs/smart_measurement_v8_en.md
# ابحث عن: "Body Tracking"
```

---

**Good Luck! 🍀**

